
  <%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="eventform">
  <br>
  <br>
  <br>
  <div class="large-4 large-centered columns field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="large-4 large-centered columns field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
    <div class="large-4 large-centered columns field">
    <%= f.label :time %><br>
    <%= f.time_select :time %>
  </div>
  <div class="large-4 large-centered columns field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="large-4 large-centered columns actions">
    <%= f.submit %>
  </div>
<% end %>
</div>

